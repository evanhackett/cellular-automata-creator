optimize for mobile.

modularize. see evernote note about pulling out modules.

add examples tab to autopopulate the textarea with working CA's.

user controls for:
  animation speed
  start setup (at least the probability of a given boolean)
  CA resolution, as in cell size on the canvas. CA_RESOLUTION value.

add mode-switch to enable 1-dimensional mode or switch back to 2D mode.
This would allow users to make 1D CA's, where each row depends on previous row.
